# Scalable-Logo-Clustering-Engine
An asynchronous Python pipeline for brand logo extraction and visual similarity grouping using Perceptual Hashing (pHash) and Disjoint Set Union (DSU). Optimized with Docker.
